#include<stdio.h>
main()
{
	
	int area,l,w;
	
	printf("Enter the value of length:");
	scanf("%d",&l);
	
	printf("Enter the value of width:");
	scanf("%d",&w);
	
	area = l * w;
	
	printf("Area of rectangle: %d",area);
	
}
